import React from 'react';
import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
    Button,
    Alert
  } from 'react-native';

const InfoScreen = ({navigation}) => {
    return (
        <View >
          <Text>Info</Text>
        </View>
      );
}

export default InfoScreen;